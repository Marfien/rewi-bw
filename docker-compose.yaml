services:
  minecraft-server:
    build:
      dockerfile: Dockerfile
      context: .
    stdin_open: true
    tty: true
    volumes:
      - ./data:/data
    ports:
      - 25565:25565
    environment:
      EULA: true
      AIKAR_FLAGS: true